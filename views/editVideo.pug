//- extends layouts/main.pug
extends layouts/main

block content
    .form-container
        form(action=routes.editProfile,method="post")
            //-lable(for="Video") Video File
            //-input(type ="file",id="video",name="video")
            input(type="text",placeholder="Title",neme="title")
            input(type="email",placeholder="Description",name="description")
            input(type="submit",value="Update Video")
        a.form-container__link(href=routes.users+routes.upload) Delete Video